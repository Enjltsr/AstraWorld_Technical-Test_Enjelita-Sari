version: '3.8'

services:
 mysql:
   image: mysql:8
   environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_DATABASE: maju_jaya
   ports:
    - "3307:3306"
 etl:
  build: .
  depends_on:
   - mysql
  volumes:
   - ./data:/app/data
   - ./archive:/app/archive